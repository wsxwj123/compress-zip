# PROJECT 状态账本 — compress-zip

> 唯一写者：主会话。子代理只读，Bug 通过返回报告回传由主会话登记。
> 走 dev-flow 全流程（标准任务）。

## 阶段进度
- 01 立项：✅ 完成 @3f974aa（BRIEF 定稿，关键决策已确认）
- 02 方案：✅ 卡点1已确认（方案定稿、盲审4重要问题已修、依赖已批准、带密码rar取舍接受）
- 03 测试设计：✅ 卡点2已确认，62条黑盒验收测试锁定 @3af6b92（LOCK 已落盘）
- 04 开发：✅ 完成（feature/core：czip.py + zipcrypto.py + 2快捷操作 + install.command；验收61/62过、1skip合理、单测25过）
- 05 验收：✅ 第2轮裁判合格（64条63过1跳，头号中文6/6+数据安全2/2达标）；BUG-01~03已修 @654cd85
  - 第1轮 code-review 发现致命删数据bug→打回；第2轮修复后重判合格
  - **但此为旧解压逻辑基线**——用户提出解压逻辑需求变更（见下），暂停在此，不进06
- 🔄 解压逻辑重构（需求变更，厘清完成，待调研+回02方案）
  - 压缩：多选→默认命名"归档.zip"（不再用父目录名）
  - 解压三选项：①当前目录=铺开 ②指定目录=铺开 ③归档文件夹=包一层"归档"文件夹
  - 同名：默认自动改名 xxx-1，不弹窗（用户最终决定，非最初的"弹窗问"）
  - 调研完成：主流工具(Unarchiver/Keka)用"智能默认"(单顶层项铺开/多顶层项套壳)，比手选三选项更好；漏了Zip Slip安全/反斜杠/外部带壳包/顶层项改名
  - 必做(不问用户直接纳入)：Zip Slip 路径校验、反斜杠归一化、不写.DS_Store/__MACOSX进包、改名作用在落地顶层项
  - 用户定：智能默认+保留手选；BRIEF已更新；PLAN/INTERFACE已按新需求更新
  - 盲审致命(symlink逃逸)+3重要+2建议已修订；✅卡点1已确认（压缩命名保持"归档"——反向拷问后用户维持，空包=报错）
  - 下一步：重写解压相关验收测试（智能布局/软链接拒绝/空包/顶层改名/Zip Slip全格式）→重新锁定→改实现→重新验收

## 开发期决策/待用户知晓
- INTERFACE §2.3 解压避让规则与锁定测试冲突 → 判定以测试为准：默认原地解压不避让、显式 --dest 才避让；已修订 INTERFACE 对齐 @131b6ed
- ⚠️ 已知局限（卡点3告知用户）：默认原地解压外部"无顶层前缀扁平包"到已存在同名目录时可能覆盖同名文件；本工具自压的包无此问题
- 快捷操作 .workflow 已 plutil/zsh 语法校验过，但能否被访达正确加载触发需用户本机 GUI 实测（07 收尾）

## 待办
- [ ] #T1 02 方案：格式调研（sonnet）+ 技术栈调研（opus）→ PLAN + INTERFACE → 盲审 → 卡点1
- [ ] #T2 03 测试设计：黑盒验收测试（重点=中文 UTF-8 标志位、加解密、同名避让）→ 卡点2
- [ ] #T3 04 开发：命令行内核 + 快捷操作接入
- [ ] #T4 05 验收 + 05.5 安全审计
- [ ] #T5 06 发布：GitHub 公开仓库 + 教程
- [ ] #T6 07 收尾：用户本机实测右键压缩/解压

## Bug 台账
| 编号 | 现象 | 状态 | 发现者 | 修复 commit |
|------|------|------|--------|------------|
| BUG-01 | 解压失败(密码错/损坏/tar-slip)会删掉同名已有目录→丢数据，违反"绝不删除"红线 | 修复中 | code-review质量抽查 | — |
| BUG-02 | 7z损坏包误判密码错退4(应退1) | 修复中 | code-review | — |
| BUG-03 | encrypt_dirs死参数+注释不符 | 修复中 | code-review | — |
| BUG-04 | 7z可能不跟随symlink(违背§1.3.1) | 待确认 | code-review | — |

**关键教训**：裁判(只看行为测试)判合格，但代码有致命删数据bug——验收测试漏测"失败时源文件是否保留"。code-review 补盲区的价值在此坐实。测试盲区已补(2条数据安全测试)。

## 关键决策（立项定）
- 交付形态：命令行内核 + macOS 快捷操作（不做 GUI App，不签名公证）
- 格式：压缩 zip/7z/tar.gz；解压广覆盖含 rar；不做 rar 创建
- 加密：AES-256 + ZipCrypto 两种，压缩时选
- 头号目标：中文名 Windows 解压不乱码（UTF-8 + 标志位，可自动验证，无需真 Windows）
- 原文件：绝不删除/覆盖，同名改名避让
- 发布：GitHub 公开仓库
- 格式调研用 sonnet（用户指定），其余调研/审查/裁判用 opus
